import React from 'react';
import Typography from '@material-ui/core/Typography';

class About extends React.Component {
    render() {
        return (
            <Typography variant="h2">Ini About</Typography>
        );
    };
}

export default About;